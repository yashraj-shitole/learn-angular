#!/usr/bin/env node
// Placeholder script to satisfy the repository pre-commit hook during
// local development when the real generator is not present.
// It intentionally exits with success so commits are not blocked.
process.exit(0);
